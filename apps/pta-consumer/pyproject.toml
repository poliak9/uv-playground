[project]
name = "pta-consumer"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["pta-shared"]

[project.scripts]
pta-consumer = "pta_consumer.main:main"

[build-system]
requires = ["uv_build>=0.11,<0.13"]
build-backend = "uv_build"

[tool.uv.sources]
pta-shared = { workspace = true }

